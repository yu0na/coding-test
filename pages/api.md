# 데이터 및 API
### json-server 사용 필수

- 과제에서는 <strong>json-server를 사용한 Mock API 서버</strong>가 제공됩니다.
- 데이터는 파일을 직접 참조하지 않고, <strong>반드시 API 호출을 통해서만 조회</strong>해야 합니다.
- 전달된 `package.json`에 포함된 아래 명령어로 json-server를 실행할 수 있습니다.

    ```zsh
        npm run server
    ```
